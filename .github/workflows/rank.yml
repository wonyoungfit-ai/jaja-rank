{"name":"Fetch Pet Rank","on":{"schedule":[{"cron":"*/30 * * * *"}],"workflow_dispatch":null},"permissions":{"contents":"write"},"jobs":{"fetch":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"run":"curl -sf -A "Mozilla/5.0 Chrome/124.0.0.0" "https://sa000001.com/sa2/info/petrank.php" -o petrank.html"},{"run":"git config user.name bot && git config user.email bot@users.noreply.github.com && date -u +'%Y-%m-%d %H:%M UTC' > petrank_time.txt && git add petrank.html petrank_time.txt && (git diff --staged --quiet || (git commit -m rank-update && git push))"}]}}}
